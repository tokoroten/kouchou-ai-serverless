// 次世代版(インタラクティブ再クラスタリング)のアルゴリズム解説ページ。
// 実装の一次資料は docs/INTERACTIVE_DESIGN_MEMO.md / REVIEW.md と src/phase2/。

const sectionStyle: React.CSSProperties = { marginBottom: 8 };

function Formula({ children }: { children: React.ReactNode }) {
  return (
    <div
      style={{
        fontFamily: "ui-monospace, monospace",
        background: "var(--bg-subtle, #f6f8fa)",
        border: "1px solid var(--border)",
        borderRadius: 6,
        padding: "8px 12px",
        margin: "8px 0",
        overflowX: "auto",
      }}
    >
      {children}
    </div>
  );
}

export function Phase2AboutPage() {
  return (
    <div style={{ maxWidth: 860 }}>
      <h1>次世代版のアルゴリズム解説</h1>
      <p className="note">
        通常版(UMAP + KMeans + ward の固定階層)と違い、次世代版はクラスタを「固定の分類」ではなく
        <b>重み付けから都度生成される『ビュー』</b>として扱います。スライダーを動かすたびに LLM
        を呼び直すのではなく、前処理で作った素材(タグ・賛否分布・候補辺)の<b>重み付けだけ</b>
        を変えて再クラスタリング・再レイアウトします。
      </p>

      <div className="card" style={sectionStyle}>
        <h2>全体の流れ</h2>
        <ol>
          <li>
            <b>意見抽出 + 構造化属性付与</b>(生コメントから・コメント1件 = チャット1回): 各コメントを
            意見単位に分解しつつ、<b>同じ呼び出しで</b>各意見に <b>スタンス(7段階の賛否分布)</b>・<b>トピックタグ</b>
            (何について話しているか)・<b>理由タグ</b>(なぜそう思うか)を付ける。
            <br />
            <span className="note">
              次世代版は通常版パイプラインに依存せず、この専用の投入口で独立に処理します(通常版の
              実行は不要)。抽出と分類を1コールに束ねているので、意見ごとに分類を呼び直していた旧方式より
              呼び出し回数が大幅に少なくなります。
            </span>
          </li>
          <li>
            <b>埋め込み</b>: 各意見文をベクトル化する(次世代版側で独立に実行。ブラウザ内ローカル埋め込みも可)。
          </li>
          <li>
            <b>コードブック構築</b>(2パス): 全意見から出たタグを集約し、表記ゆれを LLM
            で統制語彙(コードブック)に正規化して、各意見を疎なタグベクトルにする。
          </li>
          <li>
            <b>候補辺グラフの構築</b>(1回だけ・キャッシュ): 意見間の「つながり候補」を作り、
            辺ごとに意味・トピック・スタンス・理由・属性の類似度を保存しておく。
          </li>
          <li>
            <b>インタラクティブな再重み付け</b>(スライダー操作のたび・LLM 不要):
            保存済みの類似度をスライダーの重みで合成して UMAP のウォームスタートでレイアウトを滑らかに更新し、
            収束したらその 2D 配置を連結制約付き Ward で K クラスタに切る。
          </li>
        </ol>
        <p className="note">
          ステップ 1〜4 が高コストな前処理(API 課金あり・コメント単位でチェックポイント保存)、ステップ 5
          はブラウザ内の計算だけなので何度動かしても無料です。
        </p>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>1. 意見抽出 + 構造化属性付与 — スタンス・トピック・理由</h2>
        <p>
          生コメントを意見に分解する抽出と同時に、各意見について LLM が次を出力します(コメント1件= チャット1回・
          <b>コメントごとにチェックポイント保存</b>され、中断しても再開可能):
        </p>
        <ul>
          <li>
            <b>スタンス</b>: 「明確な反対〜明確な賛成」の 7 段階 + 不明の<b>確率分布</b>。 1
            点のラベルではなく分布で持つことで、曖昧な意見や両論併記を潰さずに扱えます。 プロンプトには「非反対 ≠
            賛成」(二重否定)や「引用された他者の意見を筆者の意見に しない」(引用検出)のルールを含みます。
          </li>
          <li>
            <b>トピックタグ</b>: その意見が何について話しているか(例:「AIの権利付与」「雇用への影響」)。
          </li>
          <li>
            <b>理由タグ</b>: 賛否の根拠(例:「安全性への懸念」「コスト」「倫理的問題」)。 トピックとは独立の空間で、
            <b>賛成派と反対派が同じ理由から正反対の結論を出している</b>
            ケースを束ねられるのがポイントです。
          </li>
        </ul>
        <p>
          タグは自由記述で出てくるため、2 パス目で全タグを集計して LLM に統制語彙(コードブック)へ
          正規化させます。各意見はコードブック上の重み付き疎ベクトル(トピック用・理由用の 2 本)になります。
        </p>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>2. 候補辺グラフ — 「つながりうるペア」を先に全部作る</h2>
        <p>
          全ペア(N²)の類似度をスライダー操作のたびに計算するのは重いので、
          <b>各観点での近傍(kNN)の和集合</b>を候補辺として先に 1 回だけ作ります:
        </p>
        <Formula>候補辺 = kNN(意味) ∪ kNN(トピック) ∪ kNN(スタンス) ∪ kNN(理由)</Formula>
        <p>
          和集合にするのが重要で、たとえば「意味は遠いがスタンスが同じ」ペアも候補に残ります (意味だけの kNN
          だと、スタンスの重みを上げても繋ぐ辺が存在せず分離できません)。 各辺には観点別の類似度を保存します:
        </p>
        <ul>
          <li>意味: 埋め込みベクトルのコサイン類似度</li>
          <li>トピック / 理由: タグ疎ベクトルのコサイン類似度</li>
          <li>
            スタンス: 7 段階を順序付きとみなした累積分布の L1 距離ベース
            (「賛成」と「条件付き賛成」は近く、「賛成」と「反対」は遠い)
          </li>
          <li>属性: 数値属性は範囲正規化した距離、カテゴリカル属性は一致/不一致</li>
        </ul>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>3. 重み付け — 結合ベクトルと等価な辺の重み</h2>
        <p>
          考え方は「埋め込みベクトルに、賛否などの特徴ベクトルを √重み でスケールして<b>結合</b>した
          ベクトルで距離を測る」です。結合ベクトルの二乗距離は観点別の二乗距離の重み付き和になるため:
        </p>
        <Formula>
          d² = Σ wᵦ · dᵦ² = (Σ wᵦ) · (1 − 加重平均類似度)
          <br />
          辺の重み = 加重平均類似度 = Σ wᵦ·simᵦ / Σ wᵦ
        </Formula>
        <p>
          つまり保存済みの観点別類似度をスライダーの重みで加重平均するだけで、結合ベクトルの距離と
          単調に対応する辺の重みが得られます。<b>重み 0 の観点は分離に一切使われず、大きくすれば 支配的になる</b>
          、という直感どおりの挙動になります。
        </p>
        <h3>トピックゲート(focus + context)</h3>
        <p>
          スタンスと理由は「同じ話題の中で」比べないと意味がありません(別トピックの賛成同士を 混ぜると壊れる)。そこで
          <b>全体ビューでもスタンス・理由は使えます</b>が、その重みは
          <b>トピック類似度でゲート</b>され、無関係な話題のペアには効きません(似た話題の中でだけ
          賛否・理由で割れる)。一方、<b>クラスタを選択</b>したときや<b>トピックで絞り込んだ</b>ときは、
          その時点で「同じ話題」の条件が構成上満たされているため、ゲートを外してその範囲を全力で分離します。
        </p>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>4. クラスタリング — 2D レイアウト上の連結制約付き Ward</h2>
        <p>
          クラスタリングは<b>特徴空間ではなく、出来上がった 2D レイアウトに対して</b>行います。高次元の重み付き
          グラフで切ってから 2D に落とすと、同じクラスタが投影で散らばって凸包が破綻し読めなくなるため、
          <b>特徴がすでに視覚的に解けている場所(レイアウト)で切る</b>方が正しい、という判断です(次世代版の独自性は
          クラスタリング手法ではなく、§5 の「特徴重み付きで動く地図」の側にあります)。
        </p>
        <p>
          手法は <b>連結制約付き Ward 凝集</b>: レイアウトが収束したら、その 2D 点のドロネー三角形分割 (O(n log
          n))で隣接を与え、<b>隣接クラスタ対だけ</b>を Ward コスト(併合で増える分散 =
          sᴬsᴮ/(sᴬ+sᴮ)·|重心差|²)が小さい順に貪欲に併合して樹形図を作ります。連続した意見の雲を
          <b>バランスよくコンパクトに分割</b>できるのが利点です(意見雲は隙間の乏しい連続塊なので、隙間で切る
          単連結/最小全域木だと「1 個の巨大クラスタ + 外れ値の小片」に潰れてしまいます)。
        </p>
        <p>
          <b>「クラスタ数」スライダー</b>は、この樹形図を切る位置(＝安い方から何回併合するか)を変えるだけ
          (樹形図は不変)なので、<b>UMAP を回さず即時</b>に反映されます。人が一度に把握できる数へ動的に絞れます。
          樹形図の再構築は重い(とはいえ Worker 内)ので、<b>レイアウト収束時に 1 回だけ</b>行い、途中のフレームでは
          切り直しません(<b>「今すぐ切り直す」</b>で手動再構築も可能)。
        </p>
        <p>
          スライダー操作の前後でクラスタの対応を取るため、<b>メンバーの Jaccard 類似度</b>が高い旧クラスタから ID
          と色を引き継ぎます(色が全部入れ替わって追えなくなるのを防止)。ラベルは支配的なトピック・スタンス・
          理由タグからのテンプレート生成(無料)で、必要なときだけ LLM 解説を呼べます。
        </p>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>5. レイアウト — UMAP のウォームスタート</h2>
        <p>
          配置は本物の UMAP です。候補辺上の結合距離 d = √(1 − 辺の重み) から各点の kNN を作り、 umap-js の{" "}
          <code>setPrecomputedKNN</code> に渡して正規の fuzzy simplicial set 構築から実行します。ポイントは
          <b>ウォームスタート</b>: 最適化開始前に内部の座標配列を「現在の表示座標」で上書きすることで(Python 版 UMAP の{" "}
          <code>init=array</code> 相当)、スライダーを動かしても点が飛び散らず、 今の配置から連続的に変形します。
        </p>
        <ul>
          <li>
            表示座標は重心 + RMS 半径で正規化(UMAP はセンタリングを持たず全体が漂流・伸縮するため、
            見た目のスケールを固定する)
          </li>
          <li>「X軸=賛否」は最適化ステップの後に X 座標を賛否スコアへ緩やかに引き寄せるナッジ (左=反対、右=賛成)</li>
          <li>計算は Web Worker 内で ~30fps、メインスレッドを塞ぎません</li>
        </ul>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>6. トピック絞り込み(ドリルダウン)</h2>
        <p>
          トピックが混在したままの全体レイアウトでは、スタンス等での分離結果が読みにくくなります。
          そこでコードブックのトピック、または選択中のクラスタで表示範囲を絞り、
          <b>その部分集合だけを全キャンバスで再レイアウト</b>できます(候補辺も部分集合化)。
          絞り込んだ範囲は「同じ話題」の条件を満たすので、クラスタを選択しなくても
          スタンス・理由・属性のスライダーが直接使えます。「全体に戻る」で絞り込み前の座標に復元します。
        </p>
        <p className="note">おすすめの使い方: トピックで絞る → スタンスで賛否に割る → 理由で根拠ごとに割る。</p>
      </div>

      <div className="card" style={sectionStyle}>
        <h2>通常版との違い(まとめ)</h2>
        <div style={{ overflowX: "auto" }}>
          <table>
            <thead>
              <tr>
                <th />
                <th>通常版</th>
                <th>次世代版</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>クラスタ</td>
                <td>固定の階層(UMAP 座標に KMeans + ward)</td>
                <td>重み付きレイアウト → 2D を連結制約付き Ward で K クラスタに都度生成</td>
              </tr>
              <tr>
                <td>使う特徴</td>
                <td>埋め込みのみ</td>
                <td>埋め込み + トピック / スタンス / 理由タグ + 回答者属性</td>
              </tr>
              <tr>
                <td>操作コスト</td>
                <td>再実行(ラベルは LLM)</td>
                <td>スライダー操作は LLM 不要・リアルタイム</td>
              </tr>
              <tr>
                <td>ラベル</td>
                <td>LLM 生成</td>
                <td>テンプレート生成(必要時のみ LLM 解説)</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div className="row" style={{ marginTop: 12 }}>
        <button type="button" onClick={() => history.back()}>
          戻る
        </button>
        <a href="#/phase2">プロジェクト選択へ</a>
      </div>
    </div>
  );
}
